#include <iostream>

using namespace std;

int main()
{
	// 	Задачи по главе 41: Скрытие функционала, множественное наследование и виртуальные базовые классы
	// Задача 1: Скрытие функционала базового класса
	// Создайте два класса: Base и Derived. В классе Base реализуйте метод printMessage(), а в классе Derived переопределите этот метод. При этом метод в классе Derived должен скрывать метод в классе Base. В функции main создайте объект производного класса и продемонстрируйте вызов скрытого метода с помощью оператора разрешения области видимости ::.
	// Шаги:
	// 1.  Создайте класс Base с методом printMessage().
	// 2.  Создайте класс Derived, который будет скрывать метод printMessage().
	// 3.  В функции main создайте объект класса Derived и вызовите метод из базового класса с использованием Base::printMessage().
	// Ожидаемый вывод:
	// Сообщение из Derived
	// Сообщение из Base

	// Задача 2: Множественное наследование и проблемы ромбовидного наследования
	// Создайте два класса Base1 и Base2, которые оба наследуют от общего класса Base. Затем создайте класс Derived, который наследует от Base1 и Base2. В каждом из базовых классов объявите атрибут value, и в классе Derived продемонстрируйте проблему ромбовидного наследования, при которой доступ к атрибуту value требует явного указания, от какого класса его брать.
	// Шаги:
	// 1.  Создайте классы Base1 и Base2, каждый с атрибутом value.
	// 2.  Создайте класс Derived, который будет наследовать от Base1 и Base2.
	// 3.  В функции main продемонстрируйте проблему с доступом к атрибуту value.
	// Ожидаемый вывод:
	// Base1 value: 20
	// Base2 value: 30

	// Задача 3: Виртуальное наследование и решение проблемы ромбовидного наследования
	// Модифицируйте предыдущий пример, добавив виртуальное наследование. Используйте виртуальное наследование для класса Base в классе Base1 и Base2, чтобы гарантировать, что при создании объекта класса Derived будет создан только один экземпляр класса Base.
	// Шаги:
	// 1.  Внесите изменения в классы Base1 и Base2, чтобы они виртуально наследовали от Base.
	// 2.  В функции main продемонстрируйте, что теперь при доступе к атрибуту value используется только один экземпляр класса Base.
	// Ожидаемый вывод:
	// Base value: 10

	// Задача 4: Конструкторы и деструкторы при множественном наследовании
	// Реализуйте два базовых класса Base1 и Base2, каждый с собственным конструктором и деструктором. Затем создайте класс Derived, который наследует от обоих базовых классов. В функции main создайте объект класса Derived и продемонстрируйте порядок вызова конструкторов и деструкторов.
	// Шаги:
	// 1.  Создайте классы Base1 и Base2 с конструкторами и деструкторами.
	// 2.  Создайте класс Derived, который наследует от Base1 и Base2.
	// 3.  В функции main создайте объект класса Derived и посмотрите на порядок вызова конструкторов и деструкторов.
	// Ожидаемый вывод:
	// Конструктор Base1
	// Конструктор Base2
	// Конструктор Derived
	// Деструктор Derived
	// Деструктор Base2
	// Деструктор Base1
}