#include <iostream>
#include <vector>

using namespace std;

int main()
{
	// 1.Создайте вектор целых чисел numbers со значениями {1, 2, 3, 4, 5}.
	// пользуя итераторы, выведите все элементы вектора на экран.

	// vector<int> numbers = {1, 2, 3, 4, 5};
	// for (auto it = numbers.begin(); it != numbers.end(); ++it)
	// {
	// 	cout << *it << ' ';
	// }
	// cout << endl;

	// 2.Создайте вектор целых чисел numbers со значениями {1, 2, 3, 4, 5}. Используя итераторы,
	// увеличьте значение каждого элемента вектора вдвое. Выведите измененные элементы на экран.

	// vector<int> numbers = {1, 2, 3, 4, 5};
	// for (auto i = numbers.begin(); i != numbers.end(); ++i)
	// {
	// 	*i *= 2;
	// 	cout << *i << ' ';
	// }
	// cout << endl;

	// 3.Создайте вектор целых чисел numbers со значениями {1, 2, 3, 4, 5}.
	// Используя реверсивные итераторы, выведите элементы вектора на экран в обратном порядке.

	// vector<int> numbers = {1, 2, 3, 4, 5};
	// for (auto i = numbers.rbegin(); i != numbers.rend(); ++i)
	// {
	// 	cout << *i << ' ';
	// }
	// cout << endl;

	return 0;
}